<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos de acceso a LAN requeridos para comunicación y descubrimiento con ESP32-S3 y Nexys 4 -->
    <!-- 1. INTERNET: Apertura de sockets TCP/UDP y peticiones HTTP locales hacia ESP32-S3 y Nexys 4 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 2. ACCESS_NETWORK_STATE: Verificación de estado de enlace Wi-Fi antes de iniciar descubrimiento mDNS -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 3. CHANGE_WIFI_MULTICAST_STATE: Habilitación de paquetes multicast requeridos por mDNS / NSD en la LAN -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- 4. ACCESS_LOCAL_NETWORK: Permiso de acceso a red local para Android 16/17 (Target SDK 37+) -->
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VisionSolano"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.VisionSolano"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>